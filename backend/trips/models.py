# No persistent models are required for this assessment -- trips are computed
# on the fly and returned directly to the client. Kept as a placeholder so the
# app follows standard Django app structure.
